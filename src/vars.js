var pageCount = 2
var pageContainer;
var pageBody;
var pageIcons = []
var pageIconElements = []
var SQL;
var settingsClass;
var homeP;
var settingsCustomPage;
var customPages = [];
var currentBackgroundPos;
var settingsDatabase;

window.disabledScrollElements = []

function setIcons(icons) {
    pageIcons = icons;
}

var lookSpeed = 1.5;

var pages = []

var db;


function setVar(variable, value) {
    variable = value
    
}

export {
    
pageCount,
pageContainer,
pageBody,
pageIcons,
pageIconElements,
SQL,
lookSpeed,
pages,
db,
settingsClass,
homeP,
settingsCustomPage,
customPages,
currentBackgroundPos,
settingsDatabase,
setVar,
setIcons

}